{
  "tools": [
    {
      "id": "sample-tool",
      "name": "Sample Tool",
      "description": "A sample tool to demonstrate the workflow app. Replace this with your actual n8n workflows.",
      "icon": "Wrench",
      "webhookUrl": "https://n8n.srv1300789.hstgr.cloud/webhook/sample",
      "inputs": [
        {
          "name": "message",
          "label": "Message",
          "type": "text",
          "placeholder": "Enter a message to send to the workflow",
          "required": true
        },
        {
          "name": "priority",
          "label": "Priority",
          "type": "select",
          "options": [
            { "value": "low", "label": "Low" },
            { "value": "medium", "label": "Medium" },
            { "value": "high", "label": "High" }
          ],
          "required": true
        }
      ]
    }
  ]
}
